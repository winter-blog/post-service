spring:
  profiles:
    active: local
    group:
      test:
        - common
      local:
        - common
---
spring:
  config:
    activate:
      on-profile: common
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  cloud:
    config:
      enabled: false
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  application:
    name: post-service

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka

cloud:
  aws:
    credentials:
      accessKey: AKIA5LLBLTASIX466T6F
      secretKey: 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
    s3:
      bucket: winter-blog-bucket
      post-image-prefix: post/
    region:
      static: ap-northeast-2
    stack:
      auto: false
---
spring:
  config:
    activate:
      on-profile: test
  h2:
    console:
      path: /h2-console
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: jdbc:h2:tcp://localhost/~/winter-blog-member
  jpa:
    hibernate:
      ddl-auto: create
---
spring:
  config:
    activate:
      on-profile: local
  h2:
    console:
      path: /h2-console
      enabled: false
      settings:
        web-allow-others: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/winterblog?rewriteBatchedStatements=true&profileSQL=true&logger=Slf4JLogger&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      springframework:
        jdbc: debug